services:
  - type: worker
    name: supportbot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: CONFIG_JSON
        sync: false
      - key: PYTHONUNBUFFERED
        value: "1"
